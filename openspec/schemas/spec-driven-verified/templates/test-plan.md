## Coverage

<!-- One row per scenario in this change's spec deltas, in the order they declare
     them. "Needs the board" is strict: without hardware the HIL suite reports every
     case as IGNORE and exits 0. "Status" is exists, new, or none. -->

| Scenario | How it runs | Needs the board | Status |
|---|---|---|---|
| `<requirement>` / `<scenario>` | <suite and case> | yes / no | exists / new / none |

## Not verified by this change

<!-- What the change leaves unexercised and why. A scenario with no route is
     recorded here, not hidden: it is the reason this artifact exists. If everything
     is covered, say so in one line. -->
